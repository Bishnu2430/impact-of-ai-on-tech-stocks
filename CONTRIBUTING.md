# Contributing

Thanks for your interest in this project. Suggested workflow:

1. Fork the repository and create a feature branch: `git checkout -b feat/your-feature`
2. Make changes and add tests where applicable.
3. Keep commits small and focused; write clear commit messages.
4. Open a pull request describing your changes.

Coding style:
- Use Python 3.10+ where possible.
- Follow PEP8 for formatting.

Repository hygiene:

- Keep generated analysis outputs in `reports/` when they are part of the deliverable.
- Use focused commit messages such as `feat(task2): add predictive modeling workflow`.

Repository hygiene:

- Keep generated analysis outputs in `reports/` when they are part of the deliverable.
- Use focused commit messages such as `feat(task2): add predictive modeling workflow`.
